node_modules/
.next/
dist/
.turbo/
coverage/
pnpm-lock.yaml

# Generated artifacts — formatted by their generator, not by us (YT-0031).
# Prettier reformatting them makes the drift checks fail against a diff that
# is purely whitespace. See packages/contracts/openapi/README.md.
packages/contracts/openapi/yourtal.openapi.json
packages/contracts/openapi/go/

# TASKS.md is edited live by several sessions and parsed by
# scripts/progress.mjs; a formatter reflowing it mid-edit would turn every
# checkbox tick into a format failure. docs/tasks/ is the archived v1 board.
docs/tasks/
TASKS.md

# HLS media fixture (YT-0521). An extension collision, not a preference:
# MPEG-TS segments are named `.ts`, the same extension TypeScript uses, so
# Prettier picks its TypeScript parser and fails on the binary. That breaks
# `prettier --check .` and the format CI gate for files that are video.
# ESLint needs the same exclusion and now has it in `eslint.config.mjs`:
# every package's lint script was widened from `eslint src` to `eslint .`,
# so the old note that ESLint "is scoped to src" stopped being true.
packages/media/fixtures/

# Agent git worktrees — full checkouts of this repo inside itself. Prettier
# walked into them and reported every file twice over.
.claude/worktrees/

# Rendered by gaiada-setups/tools/sync-deploy-config.py from the deploy
# registry, and written identically to every branch. Prettier reflowing it
# would make the next `--apply` see a diff and rewrite the file, so the two
# tools would fight over it on every run.
.gaiadeploy.yml
