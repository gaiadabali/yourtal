// Generated Go types for the YourTal contracts (YT-0031).
//
// This module is NOT generated — openapi-generator runs with withGoMod=false
// so that the module path and the dependency set stay under our control
// rather than the generator's. Everything beside it IS generated; see
// packages/contracts/openapi/README.md.
//
// The dependency is the generator's doing: it emits `validate:"regexp=..."`
// struct tags for string formats, which need this validator at runtime.
module github.com/yourtal/contracts

go 1.25

require gopkg.in/validator.v2 v2.0.1
