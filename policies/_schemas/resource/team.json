{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "team",
  "description": "Members, roles, invitations and store devices for one business.",
  "type": "object",
  "additionalProperties": false,
  "required": ["businessId"],
  "properties": {
    "businessId": { "type": "string" },
    "targetRole": {
      "description": "The role being granted or removed. owner is never reachable through change_role -- transfer_ownership is the only door, and it is re-auth gated.",
      "type": "string",
      "enum": ["owner", "admin", "marketer", "merchandiser", "finance", "analyst"]
    },
    "targetPrincipalId": { "type": "string" },
    "region": {
      "description": "1.5.b's F2 hard wall: the business's own region, compared against P.attr.jurisdiction.",
      "type": "string",
      "enum": ["AU", "ID"]
    }
  }
}
